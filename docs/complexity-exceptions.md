# Cyclomatic Complexity Exception Register

This document records methods that intentionally exceed the configured
cyclomatic-complexity review threshold.

## Policy

- Review threshold: cyclomatic complexity greater than 10.
- Risk threshold: CRAP score greater than 30 requires immediate test hardening
  or refactoring.
- Registered methods must retain 100% line and branch coverage. Re-run the
  repository coverage and CRAP analysis when an exception changes.

Complexity exceptions are acceptable for compiler-generated iterator machinery,
deterministic fixed-order comparison, cohesive validation/construction/state
transitions, or hot fixed-shape spatial traversal where extraction would add
indirection, allocations, or extra passes without lowering real maintenance
risk. Revisit these exceptions when coverage drops, behavior changes, or
benchmarks show a different bottleneck.

## Exception Register

| Module                                           | Method                                                                                             | Complexity | Required coverage | Rationale                                                                                                                                                          | Revisit if                                                                                                       |
| ------------------------------------------------ | -------------------------------------------------------------------------------------------------- | ---------: | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------- |
| `GridForge.Diagnostics.GridDiagnostics`          | `VisitSparseAddressCells<TVisitor>(...)`                                                           |         28 | 100% line/branch  | Bounded sparse traversal distinguishes physical and missing cells while applying bounds, state filters, visitor limits, and deterministic index order in one pass. | Sparse address semantics, query filtering, visitor limits, or traversal storage change.                          |
| `GridForge.Grids.GridWorld`                      | `TryPrepareConfiguredVoxelMask(bool[,,], GridDimensions, out VoxelIndex[])`                        |         26 | 100% line/branch  | Two deterministic mask passes validate/count, allocate the exact result size, and emit stable indices without temporary collections.                               | Sparse mask input rules, allocation strategy, or initialization performance change.                              |
| `GridForge.Grids.GridWorld`                      | `TryGetClosestGridAndVoxel(Vector3d, out VoxelGrid?, out Voxel?, GridTopologyKind?)`               |         24 | 100% line/branch  | Bounds-first seeding followed by deterministic physical-voxel comparison avoids an intermediate candidate list and preserves tie-breaking.                         | Closest-query semantics change or a pooled candidate pipeline benchmarks faster.                                 |
| `GridForge.Grids.Topology.HexPrismTopology`      | `NormalizePlanarOffset(int, int)`                                                                  |         24 | 100% line/branch  | Hex axial neighbor normalization is a compact fixed decision table; extraction would obscure the topology mapping without reducing runtime branches.               | Hex direction semantics change or the mapping moves to generated static data.                                    |
| `GridForge.Diagnostics.GridDiagnostics`          | `VisitCellsCore<TVisitor>(...)`                                                                    |         20 | 100% line/branch  | Central validation and dispatch preserve status/count behavior across targeted, all-grid, physical, and sparse-address queries without duplicate traversals.       | Query status, grid selection, or sparse missing-address policy changes.                                          |
| `GridForge.Utility.GridTraversalState`           | `TryResolveGrid(WorldVoxelIndex, out VoxelGrid?)`                                                  |         18 | 100% line/branch  | The hot cache check validates exact world and grid generations before one fallback lookup, keeping identity invariants together.                                   | Traversal caching, runtime identity, or grid ownership rules change.                                             |
| `GridForge.Grids.Topology.VoxelNeighborResolver` | `ResolveContactNeighbors(Voxel, VoxelGrid, SwiftList<Voxel>?, bool, VoxelNeighborScope, Fixed64?)` |         18 | 100% line/branch  | Hot neighbor resolution combines scope handling, tolerance expansion, pooled scratch rental, and early exit without allocations.                                   | Neighbor scopes change, profiling shows resolver overhead, or an allocation-free split preserves traversal cost. |
| `GridForge.Grids.Storage.SparseVoxelGridStorage` | `AddScanCellsInRange(...)`                                                                         |         14 | 100% line/branch  | Tight scan-cell range traversal over sparse blocks avoids materializing candidate keys.                                                                            | Sparse scan-cell layout changes or benchmarks support a lower-branch traversal.                                  |
| `GridForge.Diagnostics.GridDiagnosticChange`     | `Equals(GridDiagnosticChange)`                                                                     |         14 | 100% line/branch  | Direct fixed-field equality remains deterministic, allocation-free, and aligned with dirty-change coalescing and hashing.                                          | The dirty-change payload, equality contract, or hash inputs change.                                              |
| `GridForge.Grids.GridOccupantManager`            | `CompareTrackedOccupancies(TrackedOccupancy, TrackedOccupancy)`                                    |         14 | 100% line/branch  | Deterministic multi-key ordering avoids comparer allocation and makes snapshot precedence auditable.                                                               | Snapshot ordering changes or a shared comparer becomes available without runtime cost.                           |
| `GridForge.Grids.Topology.GridTopologyFactory`   | `TryCreate(GridConfiguration, out IGridTopology?)`                                                 |         14 | 100% line/branch  | Topology creation keeps enum dispatch and metrics validation together so invalid requests fail before allocation.                                                  | Topology kinds expand or validation moves to topology-specific constructors.                                     |
| `GridForge.Grids.ScanCell`                       | `GetConditionalOccupants(...)` (generated `MoveNext`)                                              |         14 | 100% line/branch  | Compiler-generated iterator state machine for lazy conditional occupant enumeration.                                                                               | The lazy API is replaced by an allocation-free `Into` API.                                                       |
| `GridForge.Diagnostics.GridDiagnosticChange`     | `CompareTo(GridDiagnosticChange)`                                                                  |         14 | 100% line/branch  | Fixed-order field comparison makes dirty-change precedence deterministic and auditable without comparer allocation.                                                | Dirty-change scope precedence or payload fields change.                                                          |
| `GridForge.Diagnostics.GridDiagnostics`          | `TryResolvePhysicalCell(GridWorld, in GridDiagnosticCell, out VoxelGrid?, out Voxel?)`             |         14 | 100% line/branch  | Redundant world, grid, generation, and index checks reject stale or foreign diagnostic descriptors before runtime lookup.                                          | Diagnostic identity fields or resolution rules change.                                                           |
| `GridForge.Grids.ScanCell`                       | `TryGetOccupantAt(WorldVoxelIndex, OccupantTicket, out IVoxelOccupant?)`                           |         14 | 100% line/branch  | Synchronized O(1) retrieval validates the live lock, bucket, slot, and generation in one critical section.                                                         | Ticket layout, scan-cell synchronization, or occupant storage changes.                                           |
| `GridForge.Grids.GridSpatialIndex`               | `CollectCandidates(FixedBoundVolume, SwiftBucket<VoxelGrid>, SwiftList<ushort>)`                   |         14 | 100% line/branch  | Adaptive full-scan or two-tier lookup feeds caller-owned storage and restores deterministic slot order without temporary collections.                              | Index tiers, scan threshold, query ordering, or candidate storage changes.                                       |
| `GridForge.Grids.ScanCell`                       | `AddOccupantsWithinRadius2dTo<T>(...)`                                                             |         14 | 100% line/branch  | Typed scan-cell filtering keeps type, group, radius, and occupant predicates in one pass over occupant buckets.                                                    | Typed scan allocation or predicate cost appears in profiling.                                                    |
| `GridForge.Grids.ScanCell`                       | `TryRemoveOccupant(WorldVoxelIndex, OccupantTicket)`                                               |         14 | 100% line/branch  | Ticket validation, removal, empty-bucket release, and count mutation stay together to preserve pooled ownership and state consistency.                             | Ticketing, bucket ownership, or scan-cell synchronization changes.                                               |
| `GridForge.Grids.Storage.SparseVoxelGridStorage` | `AddVoxelsInIndexRange(VoxelIndex, VoxelIndex, SwiftList<Voxel>, SwiftHashSet<Voxel>)`             |         14 | 100% line/branch  | Sparse voxel traversal visits only intersecting blocks and appends through caller-owned redundancy storage.                                                        | Sparse block indexing changes or range scans become a measured bottleneck.                                       |
| `GridForge.Blockers.Blocker`                     | `ApplyBlockageCore(bool)`                                                                          |         12 | 100% line/branch  | Guard checks, token lifetime, coverage results, and notification transitions form one auditable blocker state change.                                              | Blocker lifecycle, coverage caching, or token ownership changes.                                                 |
| `GridForge.Grids.ScanCell`                       | `AddOccupantsWithinRadius2dTo(...)`                                                                |         12 | 100% line/branch  | Untyped 2D radius filtering keeps distance, group, and occupant predicates in one pass.                                                                            | Scan filtering changes or profiling shows predicate overhead.                                                    |
| `GridForge.Utility.GridTracer`                   | `AddCoveredHexGridVoxels(...)`                                                                     |         12 | 100% line/branch  | Hex coverage walks bounded axial ranges directly and filters by voxel-center coverage without temporary geometry objects.                                          | Hex coverage rules change or a benchmarked lookup table reduces branches without allocations.                    |
| `GridForge.Diagnostics.GridDiagnostics`          | `GetCellState(Voxel)`                                                                              |         12 | 100% line/branch  | Direct flag synthesis represents combinable cell states without allocation or hidden precedence.                                                                   | Diagnostic state flags or voxel state ownership change.                                                          |
| `GridForge.Diagnostics.GridDiagnostics`          | `IsVoxelInQueryBounds(...)`                                                                        |         12 | 100% line/branch  | Integer range rejection followed by an exact topology AABB overlap keeps the hot predicate cohesive.                                                               | Query-bound semantics, topology geometry, or candidate-range generation changes.                                 |
| `GridForge.Grids.GridEventInfo`                  | `.ctor(...)`                                                                                       |         12 | 100% line/branch  | Event payload construction keeps all deterministic identity and affected-bound fields explicit.                                                                    | Event payload shape changes or construction becomes generated.                                                   |
| `GridForge.Grids.GridWorld`                      | `TryGetClosestGrid(Vector3d, out VoxelGrid?, GridTopologyKind?)`                                   |         12 | 100% line/branch  | Closest-grid lookup stays single-pass over active grids and relies on deterministic bucket order for equal-distance ties.                                          | Active-grid iteration order changes or topology filtering expands.                                               |
| `GridForge.Grids.Storage.DenseVoxelGridStorage`  | `AddScanCellsInRange(...)`                                                                         |         12 | 100% line/branch  | Dense scan-cell traversal is a direct nested loop with caller-owned redundancy checks and no candidate allocation.                                                 | Dense scan-cell layout changes or benchmarks support a flatter traversal.                                        |
| `GridForge.Grids.GridWorld`                      | `TryPrepareConfiguredVoxelIndices(IEnumerable<VoxelIndex>?, GridDimensions, out VoxelIndex[])`     |         12 | 100% line/branch  | Sparse index preparation validates, sorts, and compacts caller input once before storage initialization.                                                           | Sparse setup accepts new index sources or duplicate handling changes.                                            |

## Review Notes

- Coverage and CRAP reports are generated artifacts rather than repository
  inputs. Recreate them from the test project's coverage settings before
  reviewing this register.
- Generated iterator `MoveNext` methods should be reviewed through the source
  iterator body first; do not rewrite public lazy APIs only to reduce generated
  complexity.
- For deterministic ordering, spatial scans, and fixed-shape predicates, prefer
  benchmark-backed changes over structural edits made only to lower a metric.
- Re-run coverage and CRAP analysis after changes to scan cells, sparse storage,
  closest queries, neighbor resolution, grid registration, or tracing, then
  update this register.
